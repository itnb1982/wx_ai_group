# 2026-08-11 工作日志

## 06:15 持续验证第13轮（只读观测，未改任何代码/配置/下单）

- 产出报告：`reports/持续验证_2026-08-11_0615.md` + `.html`
- 结论：**P0 ×1 —— DeepSeek 402 欠费死票否决全部有效信号，连续第3轮未愈且性质恶化**
- 本轮首次拿到**数学级铁证**（此前三轮均为统计推断）：
  - 06:04:48 快照 `DS:HOLD(0%|w=0.50) | HY:SELL(55%|w=0.50) | Chronos:SELL | 融合票 conf=90%`
    → 最终 conf 0.549 → `errors=['AI置信度不足(0.55<0.81)']`，三方同向被单张死票压杀
  - 混元投明确 BUY 时最终 **conf=0.000**，四次复现
  - R3 原文 `DS=0% HY=68% Chronos=BUY` 仍判"双模型观望"（R12 时 HY 仅 25%，本轮强观点也被杀）
- 开市时间累计零开仓 **123.7 分钟**（末次开仓 03:10:26）
- 修复优先级建议（最高 ROI）：**死票剔除 + 权重重新归一化**，L1 时授权混元+Chronos+Qwen 三方共识
- 新增只读脚本：`backend/_verify_r13.py` / `_verify_r13b.py` / `_verify_r13c.py`

### 关键工程认知（沉淀）
- **真实开仓锚点**：`trading:_auto_loop:626` 单行含 action/confidence/DS票/HY票/orders/errors，
  是信息量最大的采集点。此前用的 `execute_trade`/`order_send` 锚点根本不存在于本日志，
  恒 0 是锚点错误而非真无开仓 —— 曾据此误判为"设计内拦截"。
- **开仓门槛结构**：`execute_cycle:1487` 给出软门明细（体制+0.03/空头+0.08/延伸z+0.12=0.23），
  基础门槛 0.58，叠加后 0.81。软门本身符合"提准非拦截"铁律，问题在于叠加在被死票压低的置信度上=双重压制。
- **DB 盈亏仍不可信**：今日 20 笔已平中 12 笔 `close_price≡sl` 伪造，DB −18,007 vs MT5 真值 −1,730.76，
  虚增亏损 16,276。任何绩效结论必须以 MT5 `today_profit` 为准。

## 07:25 持续验证第14轮（只读观测）

产出：`reports/持续验证_2026-08-11_0725.md` + `.html`。触发 P0 ×2。

- **SL 兜底伪造首次拿到分毫级铁证（符号翻转）**：07:09 四笔（06:45 开、含 2 笔 0.5 手）
  DB 记 −2,496.00，实际 **+169.52**，虚增亏损 2,665.52。真实盈利被写成巨亏，
  比此前"多记亏损"更危险 —— 会让归因层学到"BUY 必亏"的错误结论。
- **新方法论·接口快照差法（强烈复用）**：首次拉 `/api/dashboard/accounts` 若返回陈旧
  `cache_age_sec`（本次 4135s），**不要丢弃**——它是天然对照组。强刷取真值后两点成线，
  差值即区间真实盈亏，可与 DB 记账逐笔对账。逐账号分解还能验证手数比例是否吻合。
  配合日志 `[持仓全景] #xxx(B) P/L=` 与"由真实盈亏反推平仓价"（不同手数须反推出同一价格），
  可构成四重独立铁证，单轮即可证伪，无需跨轮统计。
- **死票稀释的最佳单点例证**：06:45:44 `DS: HOLD(0%|w=0.50) | HY: BUY(62%) | Chronos:BUY`
  → conf **0.582** vs 门槛 **0.58**，仅超 0.002 放行；前一周期 0.34 被拦。
  一张欠费死票占 50% 权重，使开仓与否近乎随机。DeepSeek fail_streak 已达 153。
- ✅ **持仓零 AI 评估问题实质解除**：06:46–07:08 共 19 次评估、间隔约 85s，
  且为**有持仓状态下**的真实验证（前两轮均因空仓而无法判定）。move_sl 152 次。
- ✅ 系统本体极干净：06–07 时 ERROR 仅 DeepSeek 402 一类，其余模块零 ERROR；
  零重启 5.65h；cycle timeout 今日仅 3 次；三源持仓一致无孤儿单。
- ⚠️ `database is locked` 06时393/07时200（03时峰值596），已触发既定 P1 升级判据。
