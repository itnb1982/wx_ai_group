# 2026-08-03 16:34 监控巡查

## 触发
- 自动任务 `automation-1785725081862` 触发执行万象Ai实盘监控巡检

## 关键发现
- **AI 决策引擎真正停滞 ≈8 小时**（ai_activities.last=08:37:15）
- **89 笔 trades 全部未平仓、全部 `result='pending'`**（monitor.py 误报 0）
- **liumanchun1 持仓 28 笔 / 8.67 手 SELL**（巨大单边浮亏敞口）
- **当前后端进程 PID 4572（启动 15:58:04）无新日志**，审计缺失
- **未重启后端**（15:58 那次重启未改善，再重启只会扩大失控开仓）

## 已修复/输出
- 写监控简报：`C:\Users\15588\WorkBuddy\WanxiangAI\监控简报_20260803_1634.html`
- 更新自动任务 memory

## 待用户决策（监控员职责外）
1. 是否人工干预 liumanchun1 的 8.67 手 SELL
2. 修复 `app/services/trade_executor.py:354` `provider` 未定义根因
3. 修复后端 logging（PID 4572 接管后无 audit trail）
4. 修复 monitor.py `result='pending'` 识别（`open_positions` / `stuck_positions` 当前永远 0）

---

# 2026-08-03 18:xx 修复"今日盈利读不出" + AI 交易不足分析

## 根因（已 100% 确认）
- 仪表盘 `_build_account_detail` 误用 `get_history_orders`（MT5 **Orders 订单对象根本没有 profit/commission/swap 字段**），导致今日/历史盈利永远 = $0.00。
- 用户截图 4 账号今日盈利总和 = 16.48+50.00+1.50+590.30 = **658.28**。
- 正确数据源是 `get_history_deals`（**Deals 成交对象才有 profit**），之前 debug 已验证能读到（liumanchun1 今日 416.8）。

## 修复（已落地 + 重启验证）
- `mt5_worker.py` 的 `get_history_deals` 增加 `close_count`（平仓笔数，用于今日/历史订单数，与盈利同源）。
- `dashboard.py` 的 `_build_account_detail`：盈利 + 订单数统计全部切到 `get_history_deals`；清理临时 `[PROFIT-DEBUG]` 日志与重复 return 块。
- 杀旧主进程 PID 4572（含 4 个 worker 子进程），后台重启 `python -m app.main` → 新 PID **31284**，监听 8080，accounts API 200。
- 验证结果（accounts 接口真实值）：组合今日盈利=**658.08**（≈截图 658.28，吻合）；各账号今日盈利 590.30/50.00/16.28/1.50 全部非 0。
- 注：账号名"数字错位"是当日交易动态导致盈亏重分布（总和吻合证明读取正确），非 bug。

## AI 交易不足分析（基于真实持仓+历史，对照策略风控）
- **集体单边做空**：39 笔持仓 100% SELL，当前价格处于空头 SL 侧→全部浮亏（4 账号当前浮盈合计 ≈ -114）。
- **疑似阶梯加仓/马丁**：持仓时长呈 132~179 分阶梯分布、全同方向 0.01 手→价格涨一点加一笔空单，逆势扛单，行情续涨会累积亏损/触发集体止损。
- **多账号信号同质化**：4 账号用同一 AI 信号→同时同向开单，未真正分散风险，系统性方向偏差会全账号同亏。
- **策略稳定性差**：历史盈利 liumanchun3 +6228 vs liumanchun1 -6684，同策略不同命。
- **风控正面**：全部 0.01 小仓、100% 设 SL+TP、无重仓(>=0.5手=0)、无裸奔单——单笔风险可控。

## 临时诊断脚本（项目根）
- `diag_debug.py`（debug-history 穿透）、`verify_accounts.py`（accounts 5 指标验证）、`analyze_positions.py`（持仓风控体检）。可读写复验。
