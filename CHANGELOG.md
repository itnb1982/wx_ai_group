# 更新日志

本文件记录「万象Ai 智能交易系统」对客户可见的变更。
版本号遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)，唯一权威源为项目根 `VERSION` 文件。

**版本判定口诀**

| 变更性质 | 递增位 |
|---|---|
| 客户要重新学 / 重新配（改了操作方式、配置项含义、数据结构） | MAJOR |
| 客户看到新东西（新功能、新面板、新指标） | MINOR |
| 客户没感觉但更稳（修 bug、提性能、加防护） | PATCH |

---

## [1.4.0] - 2026-08-08

V6 架构落地阶段版本。本版聚焦系统韧性与工程治理，客户侧最大感知是「后端不再无声死亡、
账号掉线会如实报警、运维具备人工紧急处置能力」。

### 新增

- **人工紧急处置窗口（E1/E2/E3/E4 四级）** — 运维可对单仓 / 单账号 / 风险域 / 全体执行
  人工平仓与封盘。系统永不自动触发，全程留痕（身份 + IP + 时间 + 理由）可追责。
  `MANUAL_HALT` 期间拒绝新开仓，但剩余持仓的 SL/TP/SmartExit 仍然有效，且只能人工解除。
- **本金权威链** — 新增 `input_capital`（客户申报本金）作为手数计算的第一权威，
  优先级 `input_capital > base_capital > balance`。解决百万级 demo 账号按余额比例
  开出巨型手数的问题。风控仍读真实余额做日亏 / 回撤熔断。
- **手数硬边界** — `clamp(raw_lots, user_min_lot, user_max_lot)` 不可被任何策略突破，
  `max_position_lots` 为第二道硬边界。
- **SignalBus 信号总线** — 跟单开仓 / 出场统一走原子占坑，根除跨线程 TOCTOU 竞态。
- **健康检查说真话** — `/api/health` 现在会因「账号失联」「DB 未就绪」「交易循环僵死」
  如实返回 `degraded`，不再出现「进程活着但一单也下不出去却显示绿灯」的静默失败。
- **版本治理** — 版本号单一权威源 `VERSION`，后端 / 前端 / 健康检查 / 关于弹窗全部读它。
- **构建指纹** — 健康检查返回 `build.version / build_time / git_commit`，
  客户报障时可精确定位代码快照。
- **客户端授权与激活体系（Phase 8）** — Ed25519 密钥对 + 机器指纹（主板/CPU/MAC）
  签发授权令牌；激活 / 心跳接口 + 执行器授权闸门（失效只停开新仓、绝不强平，
  fail-open 不连累客户）。前端激活页 + 授权徽章 + 横幅式到期提示条（不遮挡仪表盘，
  禁用遮罩墙扣押客户资产操作权）。
- **品牌视觉统一（Phase 7.2-7.5）** — 新建「神经元金环」LOGO 组件（mark/horizontal/stacked
  三变体，引用设计令牌），收编侧栏内联品牌 SVG；登录页 / 关于弹窗统一品牌；
  新增 favicon；关于弹窗展示版本 / 构建时间 / 提交号 / 授权 / 风险提示（合规三处版权之一）。
- **AI 活动流保留窗口** — `ai_activities` 全局保留最新 5000 条，超出按时间清理，
  根治长跑 demo 撑爆表 / 拖慢查询（启动期自愈 + 每 200 次写入分摊触发）。

### 修复

- **$1000 起本金 SL 钳手 100% 拒单（严重）** — `_cap_to_risk_limit` 旧约定
  `sl_points * 100` 把风险高估 100× （真实券商「1手1点≈$1」），导致小本金即便开
  券商最小手数仍判超标 → 返回 None → 100% 拒单，直接违反「多交易多赚钱」铁律。
  改为 live 本金按最小手数成交（真实风险仅 1~3%，由 L3 篮子护盾 + 日损熔断兜底不爆仓），
  客户本金 $1000 起即可正常参与。

- **智能止盈方向错配（严重）** — 原逻辑对所有持仓统一用 Chronos P90（预测上界）
  作为止盈天花板，导致 SELL 单在水下时 `current_price <= P90` 恒成立，
  被误判为「吃满利润」而在亏损状态全平。现改为方向感知：BUY 用 P90（上界）、
  SELL 用 P10（下界）。
- **Chronos 分位数据前端不可见** — `meta_quality` 此前只返回标量，前端 Sparkline
  永远显示「模型加载中」。现完整透传 P10 / P50 / P90 序列。
- **镜像出场重复执行** — 跟号 `partial_close` 存在双重执行导致持仓被平光，
  改为原子占坑后根除。
- **后端重启死循环** — `init_db` 阻塞 198s 触发 supervisor 强杀，形成
  「起不来→被杀→再起→再被杀」。改为秒级快速认输 + 后台自愈，
  期间健康检查如实报 `db_ready=false`。
- **torch 崩溃拖垮全系统** — 本地 Chronos 依赖的 torch 在 ABI 不匹配时会
  `ACCESS_VIOLATION` 直接杀死整个后端进程。改用子进程探针隔离，
  探测失败仅优雅降级为 `available=false`，主进程不受影响。

- **SPA 入口被浏览器长缓存导致白屏（严重）** — `index.html` 之前无 `Cache-Control`，
  浏览器缓存旧入口 HTML，而其引用的旧 JS 文件名（如 `index-CdZiCwzz.js`）已被
  `deploy.py` 清理 → 请求 404 → 白屏「打不开新版本」。根因：`app.mount("/", StaticFiles)`
  抢占了 `@app.get("/")` 路由，路由级 header 形同虚设。改用中间件
  `NoCacheHTMLMiddleware` 对所有 `text/html` 响应统一打 `no-cache, no-store, must-revalidate`
  （注意：必须用 `response.headers.get("content-type")` 判断，BaseHTTPMiddleware 包裹层下
  `FileResponse.media_type` 为 None）；哈希化 `/assets/*.js|css` 不受影响，照常长缓存。
  修复后浏览器每次重新核对入口，永久不再白屏。

### 工程

- 引入 pytest 分层测试套件（unit / integration / contract / live），当前 1465 通过。
- 数据库迁移统一走 Alembic，手写 ALTER 脚本全部归档禁用。
- 新增回归比对台（parity），重构决策函数前录样本、重构后逐字段比对。
- 部署收口 — 归档 28 个历史 `dist_vN` 产物到 `backups/dist_archive`，`.env` 锁定单一
  `dist`，新增 `deploy.py` 构建即部署（孤儿资产清理 + 引用校验），根除「构建成功 ≠ 客户看到新版」。

### Phase 5 · 压测与灰度（2026-08-08）

- **离线压测脚本** `backend/scripts/stress_test.py` — 直接驱动真实多租户并发原语
  （`account_lane.apply_order_jitter` / `record_fill` / `LanePool`），验证 N=1/4/10/50
  突发下单下的四类 invariants：错峰窗口正确、租户隔离（绝不串号/串资金）、
  有界吞吐（线程池不爆炸）、内存有界（环形缓冲不泄漏）。
- **压测实测全绿（N=50 / 每账号 200 单 = 10000 单突发）**：无死锁、无异常外抛；
  错峰窗口 N=1 零延迟、N≥2 落在 [0,800]ms 且封顶；归因写入开销 0.003ms/单；
  常驻内存 0.19MB。证明执行控制层在 50 客户规模下不退化。
- **灰度发布方案** `docs/phase5_stress_gray_release.md` — 客户从 1 扩容到 50+ 的
  canary 流程：阶段 0（内部测试账号）→ 1~5 → 5~20 → 20~50+，每阶段带 soak 与验收门槛；
  每客户独立接入检查清单（user_id 归属 / 风险域 / 授权激活）；平台级只熔断信号源
  不平仓、人工处置 E1~E4 分级可单账号暂停而不波及其他客户。
- **如实记录已知限制**：本地 Chronos-2 因 venv Python 3.13.14 与 torch cp313 ABI 不兼容
  而优雅降级（Task #295 探针隔离保证绝不影响交易）；本地 Qwen3-8B 副驾待装 Ollama 激活。
  当前对外副文走过渡文案「云端双脑 × 本地时序 · 三模型协同决策」，`QUAD_MODEL_READY=false`。

---

## 历史版本

1.4.0 之前的版本未维护结构化更新日志，变更记录散落于 `docs/` 下的各审计与验收报告。
