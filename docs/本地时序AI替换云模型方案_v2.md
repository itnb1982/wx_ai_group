# 万象Ai 本地时序AI架构 · 替换云模型方案 v2（综合评判版）

> 本文档是给用户参考文案（LSTM/TCN+Meta-Labeling、Chronos 换 GBM）的**独立综合方案**，结合我自己海外核实的数据源（Chronos 官方规格、Fin-Bias 论文、海外 XAU 项目、RTX 3060 Ti 实测、Windows GPU 坑）给出。两份文案仅作参考。

## 0. 用户最终指令与红线

- **红线（唯一）**：赚钱、不亏本，其他没有任何红线。
- **方向（用户定）**：用 **Chronos-Bolt-base 205M** 替换 **DeepSeek V4**（含 Hunyuan），全本地、零 token。
- **用户的洞察（我完全认同）**：
  1. DeepSeek 这么多天测试下来，只是拿**几个当前快照参数**来评判——看不到价格的时间演化，不能前置/预判未来。
  2. token 费用巨贵（每轮决策 6~8 次 LLM 调用，N=10+ 账号每天几百次，账单随账号数线性爆炸）。
  3. 本地化：省掉 token 费，还能拿历史数据做时序推理——"像一幅动画片而不是数字"，理论更准。
  4. 核心是**会不会搭建**，搭建方案很重要——所以要求我先去海外学习，本机条件已具备。

## 1. 必须纠正的误区（否则搭建走偏）

**Chronos 不是 DeepSeek 的"同岗位替换"，是"不同工种"：**

| | DeepSeek V4（被替换） | Chronos-Bolt（替换者） |
|---|---|---|
| 工种 | 综合裁判 | 价格预报员 |
| 输入 | SMC/Regime 等**当前快照特征**（文字） | 一段**历史价格序列**（数值） |
| 输出 | BUY/SELL/HOLD + 置信 + 推理文本 | 未来价格的**分位数概率分布** |
| 能力 | 多因素定性综合、解释 | 时序建模、零-shot 预报未来 |

**关键推论**：删掉 DeepSeek 之后，原来"把多个定性因素揉成决策"的**综合裁判层就空了**。这层必须我们重新搭——这正是"搭建方案很重要"的核心工程。Chronos 解决"未来价格分布"，但解决不了"该不该进、进多少、为什么"，那部分要靠我们新建的**决策合成层**。

## 2. 新架构（全本地、零 token、时序驱动）

```
MT5 行情(多周期OHLCV)
       │
       ├──────────────┐
       ▼              ▼
 SMC/Regime 硬特征   Chronos-Bolt-base 205M（本地GPU·时序预报核心）
 （结构上下文·护城河） 输入:价格序列 → 输出:未来分位概率云
       │              │
       └──────┬───────┘
              ▼
      决策合成层（新搭·核心工程）
       Chronos方向 + SMC结构 + Regime体制 → 规则化综合 BUY/SELL/HOLD+置信
              │
              ▼
      多信号互验（替代"双模型辩论"）
       M15短周期预报 + H1长周期预报 + SMC结构 三方对齐
              │
              ▼
      交易执行器（L1/L2/L3 智能平仓护盾，已落地）
              │
              ▼
      预报动画（概率云可视化·作战图）
```

**与"双模型辩论"的本质区别**：原来用两个云 LLM 互相吵架来交叉验证；现在用 **Chronos 两个周期（M15 短 + H1 长）的方向预报 + SMC 结构** 做天然三方互验——价格时序维度 + 结构维度双保险，且全本地零费。

## 3. 各层职责与搭建要点

1. **行情层（已就绪）**：复用 `mt5_worker.py` 的 `copy_rates_from_pos/copy_rates_range` 取数，无需重写。
2. **特征层（护城河·须先修偏置）**：我们的 `compute_smc` / `detect_regime` 硬特征是独有优势。⚠️ 近期 BUY-only 巨亏（-$6,743）根因是 regime 把下跌错判上涨——**在歪的方向上做预报只会精确向亏钱方向收敛**，必须先修稳再上 Chronos。
3. **预报层（Chronos base 205M）**：本地 GPU 常驻（fp16 ~400MB-1GB 显存，3060 Ti 8GB 绰绰有余）。输入价格序列 → 输出分位分布。
4. **决策合成层（新搭·成败关键）**：把 Chronos 预报方向（如"未来 60min 80% 概率在 4230-4240"）+ SMC 结构确认（需求区/OB/FVG）+ Regime 体制（趋势/震荡）→ 规则化综合出信号与置信。**这是本方案最该花工时的地方**。
5. **互验层**：M15 + H1 两周期方向须同向（或至少一个强信号），且 SMC 结构不矛盾，才放行。
6. **执行层（已落地）**：L1 默认 SL/TP、L2 反转确认、L3 篮子护盾，保持不变。
7. **可视化（升级）**：把 Chronos 的概率分位云做成"预报动画"——你想要的"动画片而不是数字"，直接喂产品"AI 工作要被看见"的诉求。

## 4. 与 v1 方案文档的差异

| | v1（上一版） | v2（本版） |
|---|---|---|
| 云模型 | 保留 DeepSeek 当质量锚点 | **删除全部云 LLM**（DS+Hunyuan），零 token |
| 模型角色 | GBM 质检引擎 → Chronos（升级质检） | Chronos 升格为**时序预报核心**，决策层重搭 |
| 模型尺寸 | small 48M（建议起步） | **base 205M**（用户定） |
| 交叉验证 | 双模型辩论 | M15+H1+ SMC 三方互验 |

## 5. 盈利闸门（守你的红线）

- **walk-forward 5 折**训练在过去、测未来；必须覆盖 **2020 新冠暴涨 / 2022 加息熊 / 2023 横盘 / 2024 牛市** 四态，否则过拟合。
- **硬门槛**：PF > 1.0、净 P&L > 0、不爆仓——任一不过关不上架。
- **低质信号处理**：优先用**仓位缩放**（降手数）而非全拦截，保交易笔数（你"过度过滤砍利润"教训的延续；但红线已明确=赚钱不亏本，凡验证能提升净盈利的硬拦截也可用）。
- **分布外风险**：Chronos 是通用时间序列预训练（能源/销售/交通），未必对 XAU 高波动/跳空/三时段最优，**必须用 XAU 历史数据实跑验证，不可盲信"预训练强"**。

## 6. 搭建计划（分阶段 + 工时）

| 阶段 | 内容 | 工时 | 状态 |
|---|---|---|---|
| **P0 环境** | 装 PyTorch(CUDA 版) + chronos-forecasting 到 managed venv | ~10-20min(后台下载) | **进行中** |
| **P1 原型** | 加载 base 205M，跑 XAU 样本预报，测延迟/显存/分位输出 | 0.5 天 | 待 P0 |
| **P2 特征对接** | Chronos 分位 → 质量分/方向分；接 SMC/Regime | 1 天 | 待 P1 |
| **P3 决策层** | 规则化合成 + M15/H1/SMC 三方互验（核心工程） | 1-2 天 | 待 P2 |
| **P4 验证** | walk-forward 四态，看是否超 GBM 0.64 / 提升净盈利 | 1-2 天 | 待 P3 |
| **P5 灰度** | 先跟号观察 → 再切主号；保留 DeepSeek 接口可一键回退 | 0.5 天 | 待 P4 |

**端到端 ~5-8 天，大部分自动化。**

## 7. 风险与回退

- **Chronos 通用预训练，XAU 分布外** → 必须 P4 实跑验证；不过关则回退或降级 small 48M。
- **8GB 显存约束**：base 205M(~1GB fp16) + 系统余量 OK，但**不能同跑本地 7B LLM**（若要本地 LLM 裁判须另配卡或 CPU 卸载，本方案已不依赖）。
- **决策合成层是新建** → 是成败关键，规则设计须保守、可回测。
- **回退方案**：保留 DeepSeek/Hunyuan 接口与 `debate_engine` 代码，验证不过关一键切回云模型，不损失现有能力。

## 8. 当前进度

- P0 后台安装中（task_id 见运行日志 `F:\WanxiangAI\chronos_install.log`）。
- 下一步：P0 完成后立即跑 P1 原型，实测 base 205M 在 3060 Ti 上的延迟与显存，确认能扛 N=10+ 账号并发后再进 P2。
