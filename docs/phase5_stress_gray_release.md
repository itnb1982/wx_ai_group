# Phase 5 · 压测与灰度发布方案

> 文档定位：V6 上线 demo 实盘前的「工程就绪度验证」+「客户从 1 扩容到 50+ 的灰度发布流程」。
> 配套可执行脚本：`backend/scripts/stress_test.py`（离线压测，无需 MT5 终端）。
> 状态：压测脚本已完成并验证（N=50 / 10000 单全绿）；本方案为发布流程规范。

---

## 1. 目标与放行门槛

V6 是**多租户 SaaS** XAUUSD 纯 AI 自动交易系统。Phase 5 要回答两个问题：

1. **系统能不能扛住 N 个客户并发？** —— 用压测脚本量化「错峰 / 隔离 / 有界 / 不泄漏」。
2. **怎么把客户从 1 个安全扩到 50+ 个？** —— 用灰度（canary）发布，每阶段带验收门槛与回滚手段。

**demo 实盘放行门槛（全部满足才可对外）**：

- [x] 压测全绿（N 最大 = 50，每账号 200 单突发）
- [x] 实况 `/api/health` 长期 NORMAL、4 测试账号在线、无熔断、ai_activities 有界
- [ ] 灰度阶段 0（内部账号 demo 实盘 N 个交易日无事故）
- [ ] kill switch / 人工紧急处置链路（E1/E2/E3/E4）演练通过

---

## 2. 压测方法论

### 2.1 为什么离线即可验

真正的多租户风险集中在三件事，且**全部收敛在 `app/core/account_lane` 的纯函数 / 有界容器里，不依赖 MT5 实时行情**：

| 风险点 | 落点 | 压测打法 |
|--------|------|----------|
| 同秒挤单（执行层错峰） | `apply_order_jitter` / `compute_jitter_ms` | 直接驱动，断言抖动窗口 |
| 线程池膨胀 | `LanePool`（硬上界 32） | N=50 并发打单，断言不死锁/不爆炸 |
| 滑点归因串号 | `ConcurrencyAttribution`（环形缓冲 cap=400） | N 账号并发写，断言只挂自己 id |

脚本导入的就是这些**真实原语**，不 mock 决策链，覆盖 N=1/4/10/50 × 每账号突发 K 单的组合。

### 2.2 四类 invariants（压测断言）

1. **错峰窗口正确**：N=1 零延迟（绝不无谓惩罚单客户）；N≥2 单次抖动均匀落在 `[0, 800]ms`，窗口下界 ≥200ms（高并发必打散），上界封顶 800ms（不随 N 无限增长）。
2. **租户隔离**：每账号的成交归因只挂自己的 `account_id`，**绝不串号 / 串资金**；无越界账号。
3. **有界吞吐**：有界车道池（硬上界 32）不会被 N=50 打爆；突发下单无死锁、无异常外抛（错峰=优化非闸门）。
4. **内存有界**：归因环形缓冲（deque maxlen=400）长跑不泄漏，常驻内存 <8MB。

### 2.3 运行方式

```bash
cd F:/WanxiangAI/backend
# 默认 N∈{1,4,10,50}，每账号突发 200 单
python scripts/stress_test.py
# 快速冒烟（N=1/4，每账号 20 单）
python scripts/stress_test.py --quick
# 指定规模
python scripts/stress_test.py --accounts 50 --orders 500 --seed 7
```

退出码 `0` = 全部 invariants 通过；非 `0` = 存在违反（末尾 FAIL 报告）。

> ⚠️ 实现注意：窗口正确性必须用纯计算 `compute_jitter_ms`（不 sleep）；`apply_order_jitter` 会真 sleep，放循环里会把测试挂成几十分钟。隔离测试用小批量（N×iso_k < 400）使全部账号落入缓冲，避免被 cap 丢弃早期样本导致误判。

---

## 3. 压测结果（2026-08-08 实测）

| 场景 | N=1 | N=4 | N=10 | N=50 |
|------|-----|-----|------|------|
| 错峰窗口正确 | ✅ | ✅ | ✅ | ✅ |
| 突发吞吐（每账号 200 单） | — | 80 单 / 3.5s | 2000 单 / 82s | **10000 单 / 85s** |
| 无死锁 / 无异常 | — | ✅ | ✅ | ✅ |
| 租户隔离（无串写） | — | ✅ | ✅ | ✅ |
| 内存有界（常驻 <8MB） | — | 0.06MB | 0.11MB | 0.20MB |
| 归因写入开销 | — | 0.001ms/单 | 0.001ms/单 | 0.003ms/单 |

**结论**：执行控制层在 50 客户突发下单下，错峰正确、租户隔离、有界吞吐、内存有界全部满足。单笔下单调度 P95 受错峰窗口约束（N=50 时 ≈757ms，属设计内），归因写入自身开销 <1ms，错峰不拖垮吞吐。

---

## 4. 灰度发布（Canary）方案

### 4.1 客户独立性铁律（对应风险三层作用域）

| 作用域 | 含义 | 灰度含义 |
|--------|------|----------|
| **L-A 账号级** | 一个交易账号 = 一个独立客户 | 每个客户数据/策略/风控**完全隔离**，独立接入执行车道 |
| **L-B 风险域级** | 同客户 1 主号 + 若干跟号 | 仅这一组可合并算资金风险（继承主号风控参数） |
| **L-C 平台级** | 跨客户 | **绝不合并客户资金**；平台只熔断「信号源」，绝不熔断客户资金 |

> 红线：任何「跨客户合并资金风控 / 用 A 的亏损停 B 的单」提案 = 作废。系统性风险来自**信号源相关性**而非资金相关性。

### 4.2 发布阶段

| 阶段 | 客户规模 | 目标 | 验收门槛（soak 3~5 个交易日） |
|------|----------|------|------|
| **阶段 0** | 内部测试账号（liumanchun1~4，已就绪） | demo 实盘验证全链路 | health NORMAL / 无熔断 / PF>1 / 无跨客户资金事件 |
| **阶段 1** | 首批 1~5 真实客户 | 验证单客户独立接入 | 每客户独立风控生效 / 授权激活 / 无串号 |
| **阶段 2** | 5~20 客户 | 验证有界池吞吐 + 滑点归因 | 高并发档滑点不显著劣于低并发档（`crowding_suspected=False`） |
| **阶段 3** | 20~50+ 客户 | 全规模 | 平台级无熔断 / 单客户异常不扩散 |

每阶段之间留 soak 窗口；任一客户触发日损熔断只影响其自身（L-A），不波及他人。

### 4.3 每客户接入检查清单

1. `mt5_accounts.user_id` 正确注册（驱动后续策略/风控归属）。
2. `strategy_configs.user_id == mt5_accounts.user_id`（否则双过滤匹配不到 → 全员回退默认 `follow_leader=True`，独立参数全失效）。
3. 主号 + 跟号风险域（L-B）合并配置正确（跟号 `follow_leader=1` 继承主号 25 个风控/平仓字段）。
4. 独立风控参数（SL/TP/手数/日损）按客户本金自适应。
5. 客户端授权激活（Phase 8：Ed25519 + 机器指纹 + 心跳）；失效只停开新仓，绝不强平。

### 4.4 熔断与回滚

- **平台级（L3）只关水龙头不抽水**：信号源系统性风险（连败/模型不可用/行情断线/鞭锯超阈）→ 只停发新开仓信号，**已有持仓交回各账号 SL/TP/SmartExit/护盾篮子**，绝不强平。口诀「只关水龙头，不抽走桶里的水」。
- **人工紧急处置（L-M）分级**：E1 单仓 / E2 单账号 / E3 风险域 / E4 全体；`halt_after` 默认强制勾选（平完 AI 又开回来）。可单账号/单域/全体暂停，**不影响其他客户**。这是第四个授权来源（人），系统永不自动触发。
- **单客户异常不扩散**：账号级隔离 + 有界池，单客户崩溃只体现在该账号 `ok=False`（见 `LanePool.map_accounts`），不拖垮其他租户。

### 4.5 客户 Onboarding 自动化（目标流程）

```
新客户提供 MT5 账号+本金
   → DB 注册 mt5_accounts（user_id）
   → 策略配置继承（follow_leader + 独立参数）
   → 授权激活（Phase 8 客户端）
   → 自动接入执行车道（LanePool）/ 参与错峰
   → 监控面板按账号独立展示（AI剧场/质量陪审团/风控事件流/账户网格）
```

---

## 5. 已知限制（如实记录，不影响 demo）

| 项 | 状态 | 影响 | 修复路径 |
|----|------|------|----------|
| **本地 Chronos-2 时序大脑** | 优雅降级（`available=False`） | 决策回退云端双脑 + SMC/Regime；动态止盈天花板/不确定性输入缺失 | 根因=venv Python 被就地升级到 3.13.14，与 torch cp313 原生扩展 ABI 不兼容 → `import torch` SEGFAULT。修复须重装匹配 Python（**注意：托管运行时 `versions\3.13.12` 连 WorkBuddy 自身工具也依赖，重装风险高，暂不碰**）。Task #295 的子进程探针隔离已保证「降级只影响本地增强、绝不影响交易」。 |
| **本地 Qwen3-8B 副驾** | 未激活（Ollama 未装） | Phase 6 L0/L2 本地 8B 副驾路径待激活 | 安装 Ollama + `qwen3:8b`（独立进程，不碰 Python 运行时）后激活；仅降级时加载，正常模式不投方向票。 |

> 当前对外品牌副文走过渡文案「云端双脑 × 本地时序 · 三模型协同决策」，`QUAD_MODEL_READY=false`，符合「四模型须真落地后才可对外」铁律。

---

## 6. demo 实盘放行核对

- [x] **压测全绿**：N=50 / 10000 单突发，错峰/隔离/有界/不泄漏全过。
- [x] **实况健康**：4 测试账号在线、health NORMAL、无熔断、主循环 ~26s、ai_activities 有界（5000 行窗口）。
- [ ] **阶段 0 soak**：内部账号 demo 实盘 N 交易日无事故（市场开放后观测）。
- [ ] **kill switch 演练**：E2/E4 人工处置链路实测「停开仓不强制平」且不波及其他客户。
- [ ] **本地模型补全（可选）**：Chronos-2 或 Qwen3-8B 任一激活后，副文再评估是否切「四模型」。
