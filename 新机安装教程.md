# 万象Ai 智能交易系统 — 新机安装部署教程

> 适用版本：万象Ai 智能交易系统 v1.4.0（商业版多租户 XAUUSD 纯 AI 交易系统）
> 适用对象：运维人员 / 客户在新 Windows 电脑上从零部署
> 编写日期：2026-08-09（含 2026-08-08 后端崩溃根因修复后的稳定配置）

---

## 0. 一句话结论

把整个 `F:\WanxiangAI` 项目目录拷到新机器 → 跑 `bootstrap.bat` 一键初始化 → 单独装好 **Moirai 的 Python 3.12 独立 venv** → 跑 `start_all.bat` 启动 → 浏览器登录 `http://127.0.0.1:8080`。

**唯独 Moirai 参考模型是个特例**（必须 Python 3.12 独立 venv，见第 5 节），其余全部自动完成。

---

## 1. 系统架构速览

| 组件 | 技术 | 说明 |
|------|------|------|
| 前端 | React 18 + Vite 5 | 已预构建到 `frontend/dist`，由 uvicorn 一体托管 |
| 后端 API | FastAPI + uvicorn | 监听 `127.0.0.1:8080`，同时托管前端与 API |
| 守护进程 | `supervisor.py` | 看门狗，后端崩溃自动重启（自愈） |
| 交易执行 | MT5 多账号 Worker | 每账号独立进程，错峰下单 |
| 参考服务 | 本地时序四模型 | Chronos-2 / TimesFM-2.5 / Time-MoE / Moirai 协奏（观测增强，不进入交易决策链） |

**架构铁律（务必理解，避免误操作）**：
- 一个交易账号 = 一个独立客户；所有模块按"租户"隔离，禁止写死账号数。
- 参考面板是**观测**，其结论**绝不**反向控制交易开关（提准非拦截）。
- 平台级熔断只关"信号源水龙头"，**绝不**强平客户持仓（只关水龙头，不抽走桶里的水）。

---

## 2. 硬件与软件前置要求

| 项目 | 要求 | 备注 |
|------|------|------|
| 操作系统 | Windows 10 / 11 64 位 | 本机实测 Windows 11 |
| GPU | NVIDIA，支持 CUDA 12.4 | 本机 RTX 3060 Ti 8GB；无 GPU 可 CPU 降级（Chronos 单次 ~300-800ms，可接受） |
| **生产 Python** | **3.11 及以上**（本机 3.13.14） | 用于项目 `.venv`：torch(CUDA) + chronos + mt5 + fastapi |
| **Moirai Python** | **必须为 3.12.x** | uni2ts 锁 numpy 1.26，Python 3.13 无预编译 wheel（详见第 5 节） |
| Node.js | 18+（**可选**） | 前端已带预构建 `frontend/dist`，改源码才需 |
| MetaTrader 5 | 终端程序 | 交易账号登录用 |
| 网络 | 首次安装需联网拉依赖 | 模型权重随项目分发，离线可用 |

> **关键认知**：这台机器上需要**两个 Python**。生产 `.venv` 用 3.11+；Moirai 独立 venv 必须用 3.12。两者互不干扰——这正是"四缺一"那次踩坑的结论。

---

## 3. 快速安装（推荐路径）

> 全程用**原生 PowerShell（管理员）或 cmd**，**不要用 Git Bash / MSYS2 / Cygwin**（见第 9 节坑 1，torch 在这些终端会段错误）。

**步骤 A — 进入项目目录**
```powershell
cd F:\WanxiangAI
```

**步骤 B — 一键初始化（自动完成 5 件事）**
```powershell
.\bootstrap.bat
```
`bootstrap.bat` 会自动：
1. 发现宿主机 Python（或读 `WX_PYTHON` 环境变量），校验版本 ≥ 3.11；
2. 在项目内创建 `.venv` 虚拟环境；
3. 安装后端依赖（`backend/requirements.txt`）；
4. 若检测到 `models/chronos-2` 且未设 `WX_SKIP_ML`，安装本地时序依赖（`requirements-ml.txt`，torch CUDA 约 2.5-3GB）；
5. 若检测到 Node 则构建前端（否则用预构建 `frontend/dist`）；
6. 运行 `init_deployment.py` 生成 `.env` / 建库 / 创建管理员账号。

> 跳过本地时序模型（不装 torch，系统降级为 SMC/Regime 规则终审，仍能交易）：
> ```powershell
> $env:WX_SKIP_ML=1; .\bootstrap.bat
> ```

脚本结尾会打印**首次登录邮箱与密码**，并写入项目根目录 `首次登录凭据.txt`。

**步骤 C — 安装 Moirai 独立 venv（见第 5 节，★★ 必做）**

**步骤 D — 启动系统**
```powershell
.\start_all.bat
```
会弹出一个 `WanxiangAI-Supervisor` 窗口并显示状态，**关掉它系统仍在后台运行**。

**步骤 E — 浏览器登录**
打开 `http://127.0.0.1:8080`，用步骤 B 拿到的邮箱密码登录。

---

## 4. 生产环境 `.venv` 详解（bootstrap 自动做，这里讲原理）

**为什么 `.venv` 不随项目拷贝**：Windows 上 `.venv\Scripts\python.exe` 只是个转发器，真实标准库/DLL 在创建它的那台机器 Python 安装目录里（记录在 `pyvenv.cfg` 的 `home=` 字段）。直接拷到别的机器会找不到路径而失败。因此交付包只带源码，venv 由 `bootstrap.bat` 在目标机现场生成。

**依赖三件套**（`backend/` 下）：
- `requirements.txt` — 核心：fastapi/uvicorn/sqlalchemy/mt5/pydantic/jose/redis/celery…
- `requirements-ml.txt` — 重型可选：torch==2.6.0(cu124) + transformers 5.x + chronos-forecasting + accelerate（约 2.5-3GB）
- `requirements-dev.txt` / `requirements-optional.txt` — 开发/可选

**解释器发现顺序**（商业可移植靠它，见 `start_all.bat` 与 `runtime_paths.find_python`）：
```
WX_PYTHON 环境变量 → 项目 .venv → 项目 venv → PATH 中 python → py launcher
```
整目录拷到任意电脑都能跑，靠的就是这个"相对路径 + 运行时发现"，**严禁硬编码盘符**。

---

## 5. Moirai 独立 venv（★★ 最易踩坑，必读）

### 5.1 为什么必须独立、且必须是 Python 3.12

Moirai 依赖 `uni2ts`，其锁定 `numpy~=1.26.0`。
- **Python 3.13**：numpy 1.26 **没有**预编译 wheel，只能源码编译 → 需要 MSVC 工具链 → 本机没有 → `pip install` 必失败（`Unknown compiler(s) "cl"`）。
- **Python 3.12**：numpy 1.26 有 `cp312` 预编译 wheel，**无需 MSVC**，一行装好。

结论：用本机已装（或新装）的 **Python 3.12.x** 建一个完全独立的 venv：`F:\WanxiangAI\models\moirai_venv_py312`。它**不污染**生产 `.venv`。

### 5.2 一键安装（推荐）

用管理员 PowerShell：
```powershell
cd F:\WanxiangAI\models
.\install_moirai_venv.ps1
```
脚本默认用 `C:\Users\15588\AppData\Local\Programs\Python\Python312\python.exe` 建 `F:\WanxiangAI\models\moirai_venv_py312`。若你的 3.12 装在别处，传参：
```powershell
.\install_moirai_venv.ps1 -PyExe "D:\Python312\python.exe" -VenvDir "F:\WanxiangAI\models\moirai_venv_py312"
```

### 5.3 手动安装（等价命令）

```powershell
# 1. 用 Python 3.12 建 venv
C:\Python312\python.exe -m venv F:\WanxiangAI\models\moirai_venv_py312

# 2. 装依赖（全预编译 wheel，无需 MSVC）
F:\WanxiangAI\models\moirai_venv_py312\Scripts\python.exe -m pip install `
  "numpy~=1.26.0" torch tsagentkit-uni2ts pandas gluonts

# 3. 校验 uni2ts 可导入
F:\WanxiangAI\models\moirai_venv_py312\Scripts\python.exe -c "import uni2ts; print('uni2ts OK')"
```

### 5.4 自动点亮

参考服务 `MoiraiP` 会**优先探测** `moirai_venv_py312`（其次 `moirai_venv`）。装好即自动点亮前端「信号源参考」面板的 **Moirai 卡片**，无需改任何代码。

### 5.5 本地权重

`models/moirai-moe-small`（或 `moirai-1.1-R-small`）已随项目分发。推理优先读本地权重，避免 HuggingFace Hub 联网超时。

---

## 6. 本地时序模型权重

`models/` 下已随项目分发四个模型目录：
```
models/chronos-2/           # Amazon Chronos-2 (120M，多变量+协变量 DXY/US10Y/VIX)
models/timesfm-2.5-200m/    # Google TimesFM 2.5 (200M)
models/timemoe-200m/        # Time-MoE (200M)
models/moirai-moe-small/    # Salesforce Moirai (447M，走 Python 3.12 venv)
```

若缺失，用下载器（**走 modelscope.cn 镜像**，huggingface.co 本机超时）：
```powershell
F:\WanxiangAI\.venv\Scripts\python.exe backend\scripts\download_ts_models.py
```

> **已知坑（HF trust_remote_code 模型）**：Time-MoE 等远程代码模型在 transformers 5.x 惰性加载下，RoPE `inv_freq` 等 `persistent=False` 的 buffer 可能未被初始化 → 拿到**未初始化内存** → 预测结果是噪声且不可复现（全程零异常零警告，是静默正确性杀手）。
> 所有 trust_remote_code 时序模型须过一遍 `backend/app/services/hf_lazy_buffer_fix.py` 的体检修复（`inspect_buffers` / `repair_rope_buffers`）。实测 Time-MoE 中招、Chronos-2 / TimesFM 健康。

---

## 7. 启动与守护（★★ 禁用 Git Bash）

### 7.1 启动
```powershell
.\start_all.bat
```
`supervisor.py` 行为：
- 每 5s 探一次 `http://127.0.0.1:8080/api/health`；
- 连续 4 次失败（~20s）判定"无声假死"→ 强杀 uvicorn 子进程并重启；
- 启动期宽限 `STARTUP_GRACE=240s`（覆盖数据库自愈预算），转入健康后才启用严格存活探测；
- 单一实例守卫（mutex + PID 锁），防止重复拉起。

### 7.2 ★★ 致命陷阱：禁用 Git Bash / MSYS2 / Cygwin 启动后端

在 Git Bash / MSYS2 / Cygwin 终端里 `import torch` 会触发 **0xC0000005 ACCESS_VIOLATION 段错误**（exit 139 / 3221225477，try/except 抓不住），而同一解释器在**原生 cmd / PowerShell** 下完全正常。

后果：Chronos 探针误判"本地模型永久不可用"并写盘 `backend/data/torch_probe_cache.json` → 本地时序模型**再也不加载**。

**若已中招**：删除 `backend/data/torch_probe_cache.json`，用原生终端重启即可恢复。

### 7.3 重启后端的标准姿势
```powershell
F:\WanxiangAI\.venv\Scripts\python.exe backend\restart_backend.py
```
它只杀 uvicorn 子进程，`supervisor` 会自动重新拉起（不杀 supervisor 本身）。

---

## 8. 验证系统是否正常

按以下顺序逐项验证（均用 curl，端口 8080）：

**① 健康检查**
```bash
curl -s http://127.0.0.1:8080/api/health
# 期望：{"status":"ok"/"degraded", ...} 且 HTTP 200
```

**② 登录拿 token**
```bash
curl -s -X POST http://127.0.0.1:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"你的邮箱","password":"你的密码"}'
# 期望：{"access_token":"...","token_type":"bearer",...}
```

**③ 参考面板四卡片（核心验收）**
```bash
curl -s http://127.0.0.1:8080/api/ts-reference/snapshot \
  -H "Authorization: Bearer <上面拿到的token>"
# 期望：models 数组含 Chronos-2 / TimesFM-2.5 / Time-MoE / Moirai，
#       且每个 available:true（四张卡片全亮）
```

**④ 启动自动交易**
```bash
curl -s -X POST http://127.0.0.1:8080/api/trade/auto/start \
  -H "Authorization: Bearer <token>"
# 期望：{"ok":true,"msg":"自动交易循环已在运行"}
```

**⑤ 密钥管理**
```bash
curl -s http://127.0.0.1:8080/api/keys/ -H "Authorization: Bearer <token>"
# 期望：{"keys":[...]} 密钥列表（注意路径带尾斜杠 /api/keys/）
```

**⑥ 前端肉眼验收**
浏览器登录后查看：
- 「信号源参考」面板四张卡片（Chronos-2 / TimesFM-2.5 / Time-MoE / Moirai）应全部可用；
- 「自动交易」开关可正常开启；
- 登录用 `1558895@qq.com` / 密码见 `首次登录凭据.txt`（演示环境）。

---

## 9. 常见问题与坑（按踩坑频率排序）

**坑 1 — Git Bash 起后端 → torch 段错误（最致命）**
症状：本地时序模型全部不可用、后端频繁异常。
原因：Git Bash / MSYS2 下 `import torch` 段错误（见第 7.2 节）。
解决：一律用原生 PowerShell / cmd / `start_all.bat`；若已写坏缓存，删 `backend/data/torch_probe_cache.json` 后用原生终端重启。

**坑 2 — Moirai 装不上（用了 Python 3.13）**
症状：`pip install uni2ts` 报 `Unknown compiler(s) "cl"` / MSVC 缺失。
原因：numpy 1.26 无 3.13 wheel。
解决：换 Python 3.12 建独立 venv（见第 5 节）。

**坑 3 — 后端反复重启循环**
复合根因（2026-08-09 已修复，记录在此供排障）：
1. `supervisor_uvicorn.log` 膨胀到 200MB+ → IO/磁盘压力 → 已加 50MB×3 代轮转；
2. Chronos 用 `torch_dtype=torch.bfloat16` 在 RTX 3060 Ti 上触发 `mat1 Float != BFloat16` 协同预测异常 → 已改 `torch.float32`；
3. Moirai 子进程 180s 超时可能挂起拖垮 uvicorn 健康探测 → 已加 60s 子进程超时 + 75s 线程池隔离 + 参考服务刷新 300s。
排障顺序：先看 `supervisor.log` 有无"健康探测连续 4 次失败"；再看 `supervisor_uvicorn.log` 有无 `mat1`/`dtype` 报错；最后看日志体积。

**坑 4 — 首次起不来 / 登不进**
原因：`.env` 未生成（`SECRET_KEY` 为空后端拒绝启动）或数据库未建。
解决：确认跑过 `bootstrap.bat` 第 5 步 `init_deployment.py`（可重复执行，不覆盖已有数据）。

**坑 5 — 前端白屏**
原因：浏览器缓存了旧 `index.html`。
解决：后端 `NoCacheHTMLMiddleware` + `deploy.py` 给资源引用注入 `?v=<mtime>` 已兜底；硬刷（Ctrl+F5）即可。

**坑 6 — 参考面板某卡片"暂不可用"**
原因：对应模型权重缺失或其 venv 未就绪。
影响：**仅该卡片不可用，主交易链路不受影响**（参考面板是观测，不进决策链）。

---

## 10. 商业化可移植要点（交付 checklist）

- ✅ 整目录拷贝到任意 Windows 电脑须能跑：所有路径走 `backend/runtime_paths.py`（`data_dir()`/`data_path()`），**严禁硬编码盘符**。
- ✅ `.venv` / `moirai_venv_py312` **不随包分发**（含机器特定 Python 路径），目标机 `bootstrap.bat` 现场生成。
- ✅ `.env` / 生产数据库 **不随包分发**（含密钥），`init_deployment.py` 现场生成管理员账号与 `.env`。
- ✅ 可移植审计：跑 `backend/scripts/audit_portability.py` 检查有无硬编码路径。
- ✅ 前端品牌/视觉单一源 `frontend/src/brand/identity.js`，禁手写；版本号单一源 `F:\WanxiangAI\VERSION`。

---

## 11. 目录结构速查

```
F:\WanxiangAI\
├─ bootstrap.bat              # 一键初始化（建 venv/装依赖/建库/管理员）
├─ start_all.bat              # 启动（原生 cmd，拉起 supervisor）
├─ build.bat                  # 打包 EXE + Inno Setup 安装包（发布用）
├─ VERSION                    # 版本号单一源
├─ backend\
│  ├─ supervisor.py           # 看门狗守护进程
│  ├─ restart_backend.py      # 标准重启（杀 uvicorn 子进程）
│  ├─ requirements.txt        # 核心依赖
│  ├─ requirements-ml.txt     # 本地时序依赖（torch CUDA，可选）
│  ├─ app\services\           # chronos_service / ts_reference_* / mt5_*
│  ├─ scripts\                # init_deployment / download_ts_models / audit_portability
│  └─ data\                   # 运行数据（.venv 不在此，由 bootstrap 生成于根）
├─ frontend\                  # React 源码 + dist（预构建产物）
├─ models\
│  ├─ chronos-2\ timesfm-2.5-200m\ timemoe-200m\ moirai-moe-small\
│  ├─ moirai_venv_py312\      # ★ Moirai 专用 Python 3.12 独立 venv
│  └─ install_moirai_venv.ps1 # Moirai venv 一键安装脚本
└─ 首次登录凭据.txt            # bootstrap 生成的管理员账号密码
```

---

## 12. 最小可行部署清单（照抄即可）

```powershell
# 1. 以管理员打开原生 PowerShell，进入项目根目录
cd F:\WanxiangAI

# 2. 一键初始化（装生产 .venv + 依赖 + 建库 + 管理员）
.\bootstrap.bat

# 3. 装 Moirai 独立 venv（Python 3.12，必须）
cd models; .\install_moirai_venv.ps1; cd ..

# 4. 启动（弹 supervisor 窗口，可关，后台常驻）
.\start_all.bat

# 5. 浏览器访问并登录
#    http://127.0.0.1:8080
#    账号密码见「首次登录凭据.txt」
```

> 记住三条红线：**① 启动只用原生 PowerShell/cmd，禁用 Git Bash；② Moirai 必须 Python 3.12 独立 venv；③ 参考面板是观测，不进交易决策链。**
