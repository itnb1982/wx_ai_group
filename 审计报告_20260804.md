# 万象 AI 交易系统 · 全方位审计报告

> 审计时间：2026-08-04 01:10（收尾审计，只读探查，未改动任何代码）
> 审计范围：前端 React/Vite + 后端 FastAPI/uvicorn + 数据库 + 多账号架构 + AI 决策工作流
> 审计结论：**系统主体健康，多账号铁律与纯AI定位均达标；发现 1 个真实 Bug（累计盈利曲线从未加载）+ 2 处死代码，均建议明日清理。无阻断性风险。**

---

## 一、架构与设计铁律遵守情况 ✅

### 1. 多账号优先铁律（最高优先级）
- **后端**：全树无 `range(4)` / 写死账号索引 / `len==4` 类判断。主号动态识别函数 `_get_trade_leader()`（trading.py:66）优先取 `is_market_primary` 标记账号，无标记则取"已连接+已启用"过滤后的首个——**账号数量 N 完全变量化**。
- **前端**：账户网格 `AccountGrid` 动态 `map` 渲染；策略页 sidebar 动态列表自动排序；行情主号机制 `is_market_primary` 字段（单账号时即唯一主号）。
- **结论**：N=1 / N=4 / N=10+ 三种心智模型均成立，无反模式。

### 2. 纯 AI 系统定位 + 设计哲学
- 决策由 DeepSeek V4（激进派）+ 混元 Hy3（稳健派）+ Meta 终裁 三方辩论驱动（`debate_engine.decide()`），无传统 EA 写死指标/方向门。
- 前端可视化：AI 工作剧场（辩论/进化/执行流三栏）、行情作战图、账户沙盘、神经网络动效——均符合"AI 工作被看见 + 高端酷炫"宗旨。

---

## 二、前端健康度（发现 1 Bug + 2 死代码）

### 🔴 发现 1【Bug·中等】`fetchEquityCurve` 从未被导入
- **位置**：`frontend/src/components/Dashboard.jsx`
  - 第 10 行 import：`{ fetchSession, fetchChart, fetchAccounts, fetchSystemHealth, fetchAIFlow, AuthError }` —— **不含 `fetchEquityCurve`**
  - 第 68 行：`const eq = await fetchEquityCurve(30)` —— 调用未导入的函数
- **现象**：运行时抛 `ReferenceError: fetchEquityCurve is not defined`，被外层 `try/catch` 静默吞掉 → `equity` state 永远为 null。
- **影响**：**累计盈利曲线从未真正加载过**。这正是之前所有版本 Hero 区域一直显示"累计盈利曲线生成中…"空白的根因（不是后端没数据——后端 `equity_curve` 端点逻辑正确，依赖 MT5 Deals 历史，数据完整可用）。
- **修复建议（明日）**：要么在第 10 行补 `import { ... , fetchEquityCurve }`，要么（因 Hero 已删除该面板）直接删掉第 66-72 行整段死调用。

### 🟡 发现 2【死代码·低】Dashboard 遗留 state
- `equity`（:33）、`liveStatus`（:29）、`aiFlowData`（:31）三个 state 已无任何组件消费（HeroAI v20 不接收任何 props），对应的 `setEquity/setLiveStatus/setAiFlowData` 调用全是冗余。
- 连同发现 1 一起清理即可。

### 🟡 发现 3【死代码·低】CSS 残留样式
- `frontend/src/styles/global.css` 第 434~476 行，约 20 条 `.he-*` / `.hero-eq` 样式（v18/v19 面板遗留），当前无任何组件引用（HeroAI v20 只含 `.hero` / `.hero-id` / `.nn-*`）。
- 不影响运行，但增加 CSS 包体积与维护混乱，建议删除。

### ✅ 已根治的历史问题
- **v19 白屏根因**：HeroAI v19 用裸标识符 `BUY` / `SELL`（应为字符串 `'BUY'` / `'SELL'`），组件渲染时 `ReferenceError` → 整页白屏。v20 已彻底删除该组件逻辑，根因消除。

---

## 三、后端健康度 ✅

- **结构**：8 个路由（auth / accounts / dashboard / keys / mt5_discover / strategy / trading）+ 15 个服务（mt5 / ai / risk / trade 等），职责清晰。
- **equity-curve 端点**（dashboard.py:440）：逻辑正确，按日聚合 `get_history_deals` 净盈亏 → 累计曲线。修复前端 import 后即可出图。
- **多账号无反模式**（见第一节）。
- **风控三层 + 跟随主号**：L1 分批止盈 / L2 反转平仓 / L3 篮子护盾；跟号 `follow_leader` 继承主号 25 个风控字段（不写回 DB）—— 已按设计落地。

---

## 四、已知技术坑（来自历史记忆，仍有效）

| 坑 | 现状 | 永久修复 |
|---|------|---------|
| Windows Defender 锁 SQLite/前端 dist | 用 `FRONTEND_DIST_DIR=dist_vN` 切换 + DB 落地 `C:\WXDB` 规避 | 管理员执行 `Add-MpPreference -ExclusionPath 'C:\Users\15588\WorkBuddy\WanxiangAI'` |
| SQLAlchemy 改字段不 ALTER 旧表 | 手 ALTER | 同左 |
| 前端 dist 缓存 | 浏览器需 **Ctrl+Shift+R** 硬刷才见新 bundle | 同左 |
| M30 周期曾漏映射 | 已修，全周期映射 | — |

---

## 五、当前运行状态

- **后端**：运行中（PID 14996，HTTP 200），`.env` 指向 `dist_v20`，前端已精简为纯身份条。
- **前端**：dist_v20 已部署，用户需 **Ctrl+Shift+R** 硬刷。
- **历史异常提示**：8/3 17:xx 监控简报曾记录"AI 决策引擎死亡 8h + 旁路失控开仓 + liumanchun1 双向 8.74 手"复合异常。当前进程运行正常，但**建议明日确认交易引擎是否已稳定、无旁路开仓残留**。

---

## 六、明日待办清单（优先级排序）

| 优先级 | 任务 | 说明 |
|--------|------|------|
| **P1** | 清理 Dashboard 死代码 + 修复 `fetchEquityCurve` import | 顺手消除"曲线从未加载"隐患；若决定不再做曲线则直接删整段 |
| **P2** | 删除 global.css 残留 `.he-*` 样式 | 约 20 行，减重 + 整洁 |
| **P3** | 确认交易引擎健康 | 查日志确认无旁路失控开仓、Meta 终裁正常流转 |
| **待定** | 若恢复"持续盈利曲线"可视化 | 需新建**独立面板**（不要放回 Hero 重复区），且先修 P1 的 import |

---

## 七、审计总结

系统整体架构稳健，核心设计铁律（多账号优先、纯 AI 驱动、高端可视化）均得到贯彻。本次审计发现的均为**非阻断性卫生问题**：1 个被 try/catch 掩盖的前端 import 缺失（导致曲线功能静默失效）、2 处历史面板遗留的死代码。无数据错误、无安全漏洞、无多账号逻辑缺陷。建议明日按 P1→P2→P3 顺序清理，不影响当前线上运行。
