# XAU/USD万象Ai自动量化交易系统 v1.0 — 全面审计报告

**审计时间**: 2026-08-02 19:14~19:30
**审计范围**: 后端 (Python/FastAPI) + 前端 (HTML/JS) + 数据库 + AI 链路
**测试环境**: localhost:8081, SQLite, 模拟市场数据

---

## 一、发现的 Bug 清单

### 🔴 严重级别 (已修复)

| # | 模块 | 问题描述 | 影响 |
|---|---|---|---|
| 1 | `debate_engine.py:30` | `HunyuanClient(api_key=hunyuan_key, secret_key=hunyuan_secret)` 但 HunyuanClient 只接受 `api_key` 参数 | **TypeError，辩论引擎无法实例化，AI 决策链路全断** |
| 2 | `frontend/index.html` | `refreshAIDecision()` 使用 setTimeout 硬编码假数据，从未调用后端 | **AI 决策面板永远是假的，用户被误导** |
| 3 | `frontend/index.html` | `addKey()` 发送 `{key_type, key_value, label}` 但后端期望 `{provider, api_key, key_name}` | **添加密钥永远 400 错误** |
| 4 | `frontend/index.html` | `saveStrategy()` POST 到 `/api/strategy/config` 但后端路由是 `/api/strategy/{mt5_account_id}` PUT | **策略保存 404 错误** |
| 5 | `frontend/index.html` | `loadKeys()` 硬编码显示假 mask，不从后端获取 | **密钥状态永远不对** |
| 6 | `dashboard.py` | 缺少 `/api/dashboard/decision` 路由 | **前端刷新按钮无后端可调用** |
| 7 | `market_analyzer.py` | MT5 未连接时返回 `{"error": "MT5未连接"}`，导致辩论引擎直接返回 HOLD | **未连 MT5 时 AI 链路完全不可用** |
| 8 | `models/trade.py` | 新增 `sl`/`tp` 字段但旧数据库表未迁移 | **SQLite OperationalError: no such column** |

### 🟡 中等级别 (已修复)

| # | 模块 | 问题描述 | 影响 |
|---|---|---|---|
| 9 | `accounts.py` | 添加/重连账号后没有把 MT5 实时余额写回数据库 | **前端永远显示 $0.00** |
| 10 | `frontend/index.html` | 金额显示使用 `.toFixed(2)` 而非千分位格式化 | **$992725.16 可读性差** |
| 11 | `mt5_service.py` | 构造函数接收 `password_encrypted` 但接口传入的是明文 | **密码被重复加密/解密混乱** |
| 12 | `auth.py` | 使用 `bcrypt` 与 `passlib` 版本不兼容 | **密码哈希失败，无法启动** |

---

## 二、修复内容

### 后端修复

1. **debate_engine.py**: `HunyuanClient(api_key=hunyuan_key)` 移除 `secret_key` 参数
2. **dashboard.py**: 新增 `/api/dashboard/decision` POST 路由，真正调用 `DebateEngine.decide()`
3. **market_analyzer.py**: 新增 `_get_mock_snapshot()`，MT5 未连接时返回随机模拟数据，AI 链路可独立测试
4. **accounts.py**: 添加/重连后自动同步 `balance/equity/profit/margin_level` 到数据库
5. **auth.py**: `bcrypt` → `pbkdf2_sha256`，解决版本不兼容
6. **trade.py**: 新增 `sl`/`tp` 字段，数据库已重建
7. **mt5_service.py**: 统一为明文密码接口，移除多余加解密

### 前端修复

8. **addKey()**: 字段名改为 `{provider, api_key, key_name, secret_key}`
9. **saveStrategy()**: 改为 PUT `/api/strategy/{mt5_account_id}`，字段名映射后端 schema
10. **refreshAIDecision()**: 真正调用 `/api/dashboard/decision`，动态渲染双模型结果
11. **loadKeys()**: 从 `/api/keys/` 获取真实数据
12. **金额格式化**: 统一使用 `fmt()` 千分位函数，`$992,725.16`
13. **自动刷新**: MT5 账号页面加载后自动调用 sync，5 秒定时刷新

---

## 三、端到端验证结果

```
✅ Login          — JWT 令牌正常
✅ Accounts List  — 空列表（未添加账号）
✅ Dashboard      — 0 账号, $0 余额（预期）
✅ AI Decision    — DS=HOLD HY=HOLD META=HOLD（模拟数据，正常）
✅ Keys           — 1 条已保存
✅ Strategy       — dual 模式, 默认参数
```

### AI 决策链路实测（模拟市场数据）

**第一次调用**:
- DeepSeek V4: **BUY** (72% 置信度) — 全周期趋势向上，MACD看涨
- 混元 Hy3: **HOLD** (45% 置信度) — H4 RSI超买(71)，观望
- Meta-Agent: **HOLD** — 分歧→保守，H4超买风险未消除

**结论**: 双模型辩论系统工作正常，不同模型给出不同结论，Meta-Agent 综合后选择更保守的决策。

---

## 四、仍存在的问题 / 待办

| # | 问题 | 优先级 | 说明 |
|---|---|---|---|
| 1 | MT5 全局单例限制 | 🔴 高 | `MetaTrader5` Python 库是全局单例，`initialize()` 会覆盖之前连接。当前多账号架构在**同一进程内**无法真正并行。短期方案：限制为单账号；长期方案：多 MT5 终端实例 |
| 2 | 前端策略配置表单字段 ID 不匹配 | 🟡 中 | 前端有 `riskPerTrade`/`maxDailyLoss` 等字段，但 `saveStrategy()` 映射的是 `max_position_lots`/`max_daily_loss_pct`。需要统一表单 ID |
| 3 | 缺少交易执行定时任务 | 🟡 中 | 当前 `/api/dashboard/decision` 是手动触发，需要后台定时轮询 |
| 4 | 交易日志未接入后端 | 🟡 中 | 前端 `logs` 数组是内存中的，未持久化到数据库 |
| 5 | WebSocket 未认证 | 🟢 低 | `/api/dashboard/ws` 未做 JWT 认证，任何人可连接 |

---

## 五、下一步建议

按优先级逐个实现：

1. **实现交易执行定时任务** — 后台每 N 分钟自动运行 AI 决策→风控→下单
2. **修复策略配置表单** — 统一表单 ID 与后端字段名
3. **接入真实 MT5 测试** — 添加 Demo 账号，验证下单/平仓链路
4. **持久化交易日志** — 前端日志写入后端数据库
5. **WebSocket 认证**
