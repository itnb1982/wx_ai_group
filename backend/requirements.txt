fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.12
httpx==0.27.2
openai==1.51.0
MetaTrader5>=5.0.60
redis==5.1.1
celery==5.4.0
websockets==13.1
aiofiles==24.1.0
cryptography==43.0.1
pyyaml==6.0.2
loguru==0.7.2
apscheduler==3.10.4
psutil==7.2.2
# --- 2026-08-08 商业化审计补齐：以下两项被代码实际 import 但此前漏登记 ---
# requests: app/services/market_data.py 等模块的同步 HTTP 客户端
requests==2.32.3
# numpy: 行情/指标/时序特征计算
numpy==2.1.3
# starlette: main.py 直接 import（中间件/响应类）。
# 它虽然是 fastapi 的传递依赖，但「我直接用了它的 API」和「它碰巧被装上了」
# 是两回事——不显式声明，将来 fastapi 换掉或升级 starlette 大版本时，
# 我们的代码会在没有任何 requirements 变更的情况下悄悄崩掉。
# 版本与 fastapi 0.115.0 的约束保持一致。
starlette==0.38.6

# 视觉图表渲染（vision_chart.py 直接依赖；此前漏声明，安装 Pillow 后测试才绿）
pillow>=10.0.0
