# 模型科学规划 · 8 模型各司其职（2026-08-17）

> 用户批评（成立）：8 个模型只用 5 个，TimesFM / Time-MoE / Moirai 三个本地模型"休息"，
> 审计从未系统审视模型利用率。本轮：先用 **walk-forward 实测数据**回答"为什么之前没启用"，
> 再按数据**科学定岗**——不是摆设，是之前岗位规划错了。

---

## 一、先认账：审计盲区在哪

此前 4 轮审计（快监顺序/凭证回退/篮子方案/断连根治）全部聚焦**决策链正确性与 bug**，
从没问过一个基础问题：**"8 个模型里每个都在干什么？有没有装好没通电的？"**
你的批评一针见血。本轮补上模型利用率审计 + 科学定岗。

## 二、walk-forward 复测：时序模型的真实能力（30 天 XAUUSD M15，172 测试点，预测未来 2h 方向）

| 模型 | 角色 | 方向准确率 | 净点数(2h持有) | 结论 |
|---|---|---|---|---|
| numpy 规则（现役） | 方向守卫 | 49.4% | **+17811** | 唯一正收益（趋势惯性） |
| chronos-2（现役） | 时序票/风险区间 | **41.3%** | -45931 | 方向显著劣于随机！ |
| timesfm-2.5（闲置） | — | 50.0% | -10241 | 不优于随机 |
| moirai（闲置） | — | 50.6% | -2829 | 不优于随机 |
| time-moe（闲置） | — | **SKIP** | — | transformers 5.14 接口 4 处不兼容 |

**残酷事实：时序基础模型做"方向预测"全部不优于随机（含现役 Chronos-2！）**

这就是竞技场当年把它们停用的数据依据——**不是摆设，是"方向票"这条路走不通**。
但旧规划的错在于：**停用 = 全停**。正确做法是让它们干自己真正擅长的事——
**独立的风险区间估计（分位数）**，与 Chronos-2 交叉验证。

## 三、科学定岗：8 模型 × 岗位矩阵（新规划）

| 模型 | 类型 | 岗位 | 开仓 | 持仓管理 | 平仓 | 状态 |
|---|---|---|---|---|---|---|
| DeepSeek | 云 | 方向主脑+辩论 | ✅ 投票 w0.54 | ✅ position_action | ✅ L2反转 | 在岗 |
| 混元 HY | 云 | 方向主脑+辩论 | ✅ 投票 w0.46 | ✅ position_action | ✅ L2反转 | 在岗 |
| qwen2.5vl:7b | 本地 | 视觉结构票+看护 | ✅ 投票 w0.30 | ✅ VisionExit | ✅ VisionExit | 在岗 |
| qwen3:8b | 本地 | 校对员/降级副驾 | ⚠️ 常态不改票 | ❌ | ❌ | 在岗 |
| Chronos-2 | 本地 | **风险区间主** | ✅ 区间/弱方向 | ✅ 止盈目标 | ✅ smart_exit | 在岗 |
| **TimesFM-2.5** | 本地 | **风险区间交叉验证** | **✅ 区间分歧→q微调/止盈收紧** | **✅ 区间分歧** | — | **本轮启用** |
| **Moirai** | 本地 | 时序竞技场参考 | 参考面板观测 | — | — | **复测过关待接入** |
| **Time-MoE** | 本地 | — | ❌ 接口不兼容 | — | — | **待官方适配** |

## 四、本轮落地：TimesFM-2.5 风险区间交叉验证（新增 ts_cross_validate 服务）

**设计依据**（不是拍脑袋）：
- TimesFM 方向 acc 50.0%（无用），但它独立预测的分位数**与 Chronos-2 是两把不同的尺子**
- 两把尺子一致 → 区间可信 → 信号质量 **q+0.05**（提准）
- 两把尺子显著分歧 → 不确定性高 → **q-0.10 + 止盈天花板收紧到较保守端**（降损）
- **铁律：只调质量分和止盈，绝不做开仓拦截**（提准非拦截红线）

**性能实测**：常驻单例加载 3.2s（首轮）→ 纯推理 0.10s/次 → 缓存 120s
（指纹=末16根价格+长度），决策循环 60-90s 完全放得下。

**改动清单**：
- 新增 `app/services/ts_cross_validate.py`（单例+缓存+超时静默降级）
- `app/services/meta_quality.py`：`_evaluate_core` 增加 cross 参数；同步/异步入口接入
- 单测 `tests/test_ts_cross_validate.py` 6 项全过（真实推理验证分歧度语义）

**端到端验证**（真实行情 4391.72 收盘）：
- Chronos 窄区间（乐观 0.2%）→ TimesFM 分歧 0.613 → mid（谨慎）
- Chronos 宽区间（1%）→ 分歧 0.277 → high（区间可信）
- 语义正确：**Chronos 越乐观、分歧越大，系统越谨慎** ✓

## 五、Moirai：复测通过，接入方式评估

- 复测 acc 50.6% / pips -2829（方向无用，符合预期）
- 但可作**第三把尺子**：py312 venv 子进程推理（每轮 1-2s 延迟）
- 决策：**保留参考面板观测 + 离线月度复测**，不进实时决策链——
  两把尺子（Chronos+TimesFM）已覆盖区间交叉验证，第三把边际收益低、
  每轮多 1-2s 延迟不值。等 TimesFM 交叉验证跑出 1-2 周真实数据再评估是否加第三尺。

## 六、Time-MoE：为什么"装好没通电"——接口不兼容（技术实锤）

- 权重在 `models/timemoe-200m`（config + safetensors + 自写 modeling）
- transformers 5.14 有 **4 处 API 断点**：`seen_tokens`（改名 get_seq_length）、
  `from_legacy_cache`（移除）、`get_usable_length`（移除）、
  `_extract_past_from_model_output`（新增调用）→ 模型代码是 4.x 时代写的
- 逐层 monkey-patch 是在生产全局 DynamicCache 类上挂补丁 → **有污染风险，拒绝**
- 正确解法：等官方 release 或建 transformers4 隔离 venv（约 10min，收益低暂缓）

## 七、总结：对你的问题的直接回答

1. **"怎么会让三个本地模型休息？"** —— 方向预测实测全部不优于随机（数据为证），
   停用"方向票"是对的；**但岗位规划错了**，正确做法是让它们做**区间交叉验证**。
2. **"提准降损是摆设吗？"** —— 现在不是了：TimesFM 交叉验证本轮启用，
   区间分歧直接影响 q 分（提准）和止盈天花板（降损），**是加法不是摆设**。
3. **"每次审计都没发现？"** —— 认账。审计一直盯着"链路对不对"，没问"每个模型在不在岗"。
   本轮起，模型利用率 = 审计固定检查项。

## 八、待办

- [ ] 重启生效后观测 TimesFM 交叉验证真实分歧率（面板 meta_quality 新增 cross_ts 字段）
- [ ] 1-2 周后评估：分歧度高时 q 降幅是否有效降低亏损单（walk-forward A/B）
- [ ] Time-MoE 官方适配后复测启用
- [ ] Moirai 第三尺评估（视 TimesFM 效果）
