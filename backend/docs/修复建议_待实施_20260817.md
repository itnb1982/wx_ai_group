# 2026-08-17 盯盘发现·修复建议（状态更新）

> 原「待实施」→ **已完成实施（09:50）**——用户指令"先保存→盯盘验证→验证通过再修"，
> 盯盘验证（真实行情 M5 ↓↓↓↓↑ vs AI SELL 50.3% 未开仓 vs 视觉 BUY 95% 分歧）确认信号无问题 → 实施修复。

## ✅ 已完成

### 1.【P1·已实施】DeepSeek 推理链截断（06:03-09:40 共 54 次）
- **修法落地**：`deepseek_client.py` L406 `_KEEP_BARS` 收紧——M1 20→12、M5 14→12、M15 18→14、M30 20→16、H1/H4 24→18、D1 10→8（砍 ~30% 输入，保留全部 7 周期）
- 验证：编译 OK

### 2.【P2·已实施】辩论环 shadow 埋点（A/B 基线）
- `debate_ring.py` `run_debate_ring` 加 `shadow: bool=False` 参数（shadow 时忽略开关强制计算）
- `meta_agent.py` 开关关闭时仍计算 shadow（不应用），写入 decision 的 `debate_ring_shadow` 字段
- 验证：正常模式返回原值 / shadow 模式正确缩权（0.61→0.534，牛3熊3 scale 0.875）✅
- **意义**：跑 1-2 周基线后对比 `orig_conf vs scaled_conf`，无痛开启 DEBATE_RING_ENABLED

### 3.【P2·已实施✅】毫秒级跟单（方案 C）——2026-08-17 09:58 用户拍板"开搞"
- **根因（实测锁定）**：跟号 copy_order 前先跑 `_rescan_pending_verify`（反向对账，每账号 19-20 笔历史待回填，单账号 ~10s）——主号 09:18:28.802 开仓 → 2877213e 09:18:38.308 复制(+10s) → b3db40fd 09:18:46.546(+18s)
- **修法**：`trading.py` 跟号处理顺序从「对账→持仓管理→copy_order」改为「**copy_order(毫秒级)→对账→持仓管理**」——跟单本质是复制主号信号，不依赖本账号对账结果，绝不能卡在对账后面
- **安全验证**：copy_order 用 `_positions_checked`(MT5 实时)+`_is_copied`(进程内存)，不依赖本地 trades 表；`_reconcile_positions` L1110 `if ticket in live_tickets: continue` 不会误删刚复制的单
- 编译 OK，待重启生效

## 验证记录（09:47 盯盘）
- 真实行情：M5 最近 5 根 ↓↓↓↓↑（4 阴 1 阳），价格 4397.8 跌破 4400
- AI 大脑：cycle#105 SELL 50.3%（DS 62%+Chronos SELL）→ 置信 < 0.58 门槛 → **未开仓（正确）**
- 视觉：BUY 95%（M5=BUY 80%）→ 与 AI 分歧 → 正是多信号博弈设计
- 三 demo 账号空仓，今日 7 笔全赢 +$683.85 → **开单信号验证通过**

## 待重启生效
- 两项修复已落地，需用户双击 `backend\restart_task_backend.bat` 重启加载
