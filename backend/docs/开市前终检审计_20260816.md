# 开市前终检·端到端深挖审计（2026-08-16 18:14）

> 开市前最后一次全链路深挖：3 路只读审计代理并行（数据入口→决策链→仓位/平仓→MT5 端口）
> + 人工复核全部关键发现 + 运行时真实状态核查。**本轮修复 6 项，已编译验证。**

---

## 一、本轮复核确认「昨日 8 项修复全部 OK」

| 修复 | 复核结论 |
|---|---|
| P0-1 追踪止盈方向 | ✅ `_trail_tp` BUY 现价+距离只上移 / SELL 现价−距离只下移，与 modify_sl_tp 配套 |
| P0-2 rejected_signals NameError | ⚠️ **复核发现残留**（见下 P0-2a），已补修 |
| P0-3 partial 回执快照 | ✅ duplicate 回放真实 vol/profit/net_profit，_record_close 恢复记账正确 |
| P1-1 空数据 simulated | ✅ 全 TF 不足→simulated+stale，debate_engine HOLD 拦截生效 |
| P1-3 双脑置信 clamp | ⚠️ **复核发现 NaN/inf 漏洞**（见下 P1-3a），已补修 |
| P1-4 vision_exit side | ✅ provider 补 type→side + vision_exit 内兜底，双重覆盖 |
| P1-5 HALT_ALL 文档 | ✅ 文档诚实化（行为不变，有意铁律） |
| P1-9 K线时间戳 | ✅ bar time 减 offset 还原，与 position/服务器墙钟同基准 |

---

## 二、本轮新发现并修复（6 项）

### P0-2a【trade_executor.py:1888】rejected_signals fallback NameError 残留（复核补修）
- 问题：ORM 路径已修，但原生 sqlite3 fallback 的 SQL 参数仍引用未定义的 `entry_price/atr` → NameError 被吞 → fallback 静默失败，表仍写不进
- 修复：改用 `_rs_entry/_rs_atr`（与 ORM 路径同源）
- 附带修复：`_rs_saved = True` 无条件赋值 → 改为按 `_safe_db_write()` 返回值赋值（写失败时正确走 fallback）

### P1-3a【meta_agent.py:650-666】NaN/inf 置信经 clamp 变 1.0 满置信（复核补修）
- 问题：`float("nan")` 合法，`min(1.0, nan)` 返回 1.0 → ds/hy_confidence 被 clamp 成 1.0 → **门控/手数全按满置信走，风控被击穿**
- 附带：`"1.5"`（=150%）被 /100 误除成 0.015（权重消失）；`"95%"` float 抛错回退 0.5
- 修复：`_safe_conf()` 统一处理——isfinite 判空回退 0.5、strip %、>100 才 /100、最终 clamp [0,1]
- 验证：12 个边界用例全过（nan/inf/95%/150%/1.5/0.85/-5/None/空 等）

### P2【trade_executor.py:2176】主号手数=0 仍可开（L3 熔断击穿）
- 问题：主号路径无 `position_size<=0` 拦截（跟号已有），L3 熔断/降级把手数算成 0 后仍进 place_order 开 0.01 手假单
- 修复：追价缩仓后加 `if position_size <= 0: return`（与跟号对称）

### P1【trade_executor.py:3489】L2 反转平仓失败仍广播（主跟背离）
- 问题：`close_position` 后无条件 `publish_leader_exit`——主号 close 瞬时失败仍广播 → 跟号平掉、主号保留 → 背离
- 修复：成功/已不存在才广播；失败归还防抖状态待下轮重试（与 full_close 分支"失败绝不广播"对齐）

### P1【smart_exit.py:406-414】HIGH 封顶回调 TP（利润提前落袋）
- 问题：P90/P10 逐轮重算可下移，封顶钳位后 trailed_tp 可能 < current_tp（BUY）→ 判 != 即改单 → TP 被下调，违背"只上移不回调"
- 修复：封顶后加方向护栏（BUY 低于 current_tp → None；SELL 高于 → None）

### P1【trade_executor.py:3452】视觉 partial close_pct 越界 → 主平跟留
- 问题：视觉 partial 的 close_pct 只 clamp [0,1]，≥0.95 时转全平但按 partial_close 广播；跟号镜像要求 [0.05,0.95] 越界忽略 → 主平跟留
- 修复：≥0.95 改按 full_close 语义处理（与 partial 分支 vol_close>=vol_total 转全平一致）

---

## 三、审计A 误报澄清

- **"周五 22:00 判开市留周末敞口" = 误报**：STARTRADER/XM 黄金真实收盘是周五 23:55（GMT+3），周五 22:00-23:55 本就是合法交易时段，开仓无风险。真正的瑕疵只是 `_next_open_moment` 把收盘点写成 22:00 → `countdown_to_close` 显示差约 2h（纯展示精度问题，降级 P2，未改——改需动魔法数字且无安全影响）

---

## 四、遗留待评估（未修，改动面大或需回测）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| P0 | `orderflow_cvd.py:353` | 主进程裸调 mt5.initialize() 取 tick，30s/次永不 shutdown，凿穿多进程隔离 | 删该路径仅用 M1 bars 回退，或经 Worker IPC |
| P1 | `trade_executor.py:835` + `debate_engine.py:105` | 行情主号启动后永不 failover（set_market_primary 无调用点），主号熔断→永久 HOLD 直至重启 | 每 cycle 调 pick_market_primary |
| P1 | `market_analyzer.py:193` | _get_current_price 拉 7×200 根取 bid/ask（主号+每跟号每轮重复），应改用 get_tick | 轻量化 |
| P1 | `_PARTIAL_DONE` 单槽 vs 4级分批 | 浮盈锁50%先触发→TP1/2/3 全死，剩余靠追踪 SL 兜底 | 按 TP 档位多槽（需回测验证） |
| P1 | `risk_engine.py:193` | 日亏损窗口本地 naive vs worker 服务器墙钟，差 5-6h | 统一服务器墙钟切日 |
| P2 | `trade_exit.py` | commission/swap 明细恒 0（净利已含佣，审计归因断链） | worker 拆分透传 |
| P2 | `mt5_worker.py:93-98` | exec_req_ids.json 单文件被 6 Worker 覆盖写，跨重启去重失效 | 按账号分文件 |
| P2 | `meta_agent.py:994` | B门/A门惩罚过弱（×0.88/0.80 拦不住） | 阈值吸附（需回测） |
| P2 | `trade_executor.py:2203` | max_positions=0 配置恒拦且无告警 | max(1,...) |
| P2 | `dashboard.py:654` | deal.time 服务器墙钟秒直接 fromtimestamp 归日，日盈亏偏移 5h | 减 offset |
| P2 | `market_data.py:112` | VIX 未走 _guard + timeout 20s 拖慢刷新 | 复用 DXY 的 3s 硬超时 |

---

## 五、运行时真实状态（审计时点 18:14）

```
后端:  pid=22088  uptime=78min  mt5=6/6  trading=3/3  degraded=False  offline=[]
视觉:  model=qwen2.5vl:7b  runs=39  ok=30(77%)  err=''  vote=SELL 0.95
GPU0:  5478 MiB（qwen3:8b 校对员，Windows GPU1 第一张3060Ti）
GPU1:  7834 MiB（qwen2.5vl:7b 视觉，Windows GPU2 第二张3060Ti）
```

## 六、验证

- 编译：3 个改动文件 py_compile 全 OK
- 回归：追踪止盈 5/5、视觉 swing 8/8、置信边界 12/12 全过
- 运行时：健康 6/6、视觉 ok=30 连续正常

## 结论

**开市前系统处于健康状态**。本轮 6 项修复已落地（rejected_signals 双路径、NaN 置信、手数=0、反转广播、TP 回调、视觉 partial 越界），全部围绕"主跟一致性 + 风控不被击穿"——都是周一实盘会咬人的点。遗留项（主号 failover、orderflow 裸调、日亏时区等）已在案，建议作为下一迭代主题，不阻塞开市。

> 待重启生效：改动已就绪，需双击 `backend\restart_task_backend.bat` 重新加载后端。
