# 端到端深挖审计 + 业界差距对照（2026-08-17 第二轮）

> 本轮背景：用户实盘发现"智能平仓不智能"（三单 +10 回吐到 0 无 AI 干预）→ 落地**篮子级 AI 持仓管理**（position_action 协议 + 篮子回吐保护）→ 全链路复核 + 业界对照。
> 子代理额度用尽（429），审计由主链路亲自完成（静态代码核查 + 运行时核查 + 业界双源调研）。

---

## 一、本轮实施：篮子级 AI 持仓管理（用户铁律落地）

### 已实施（编译 + 单测全过，待重启生效）

| 层 | 内容 | 文件 |
|---|---|---|
| L0 协议扩展 | DS/HY prompt 加 `position_action`（hold/trim/close_all）+ 持仓处置指令（"有持仓时管理持仓与找机会同等重要"） | deepseek_client / hunyuan_client |
| L1 融合+防抖 | `_fuse_basket_action`（同向加权/分歧降权/非法回退）+ `_confirm_basket_action`（连续 2 轮确认）→ decision.basket_action 字段 | meta_agent |
| L2 执行 | `_manage_positions` 消费 close_all（全平）/trim（每笔减半，≤0.02 手不减），120s 冷却，publish_leader_exit 广播跟号 | trade_executor |
| L3 回吐保护 | 篮子浮盈峰值跟踪，峰值≥$6 即启用，回吐 ≥ max(峰值×50%, $8) → 全平锁利（**floor 用峰值判定**——+10 回吐到 +5 跌破 floor 仍保护） | trade_executor |
| 配置 | BASKET_* 8 参数 | config |
| 测试 | tests/test_basket_ai_mgmt.py 全过（融合 5/防抖 3/回吐 4） | tests |

### 自查发现并修复的 2 个真 bug（审计价值）

1. **辩论反驳轮无 position_action**：meta_agent 融合用 `deepseek_rebuttal`，但反驳轮 prompt 只有 decision/confidence/rebuttal_points——position_action 永远为空 → 融合恒 hold。**已修**：rebuttal 无则回退首轮 analysis。
2. **"平了又开"风险**：execute_cycle 先开仓后 _manage_positions——AI 确认 close_all 的轮次，先开新仓再被全平。**已修**：开仓 gate 链最前加守卫——AI 已确认平仓处置的轮次跳过开新仓，先执行持仓处置。

## 二、运行时核查（审计时点 11:57）

```
后端:  pid=79916  uptime 0.9h  mt5=6/6  trading=3/3  degraded=False  offline=[]
今日战绩: 17 笔已平  净盈亏 +53.33（demo）  在仓 0 笔
        2877213e: 3 笔 +10.31 | 主号: 7 笔 +21.96 | b3db40fd: 7 笔 +21.06
```

## 三、遗留问题现状（前两轮审计记录，本轮确认）

| # | 级别 | 问题 | 现状 |
|---|---|---|---|
| 1 | P0 | orderflow_cvd 主进程裸调 mt5.initialize（凿穿多进程隔离） | numpy 兼容已修（VoidDType），**架构问题仍在**——未修 |
| 2 | P1 | 决策链行情主号 failover 无调用点（主号死→永久 HOLD 至重启） | 未修（改动面大） |
| 3 | P1 | 日亏熔断本地 naive vs worker 服务器墙钟差 5-6h | 未修 |
| 4 | P1 | VisionExit 视觉看护不可用（0 取票静默失败） | **已加失败日志**（重启后可见真实原因）；根因疑为 Worker 反复断连 |
| 5 | P1 | Worker 反复断连（b3db40fd 两次、主号一次）→ _meta 凭证丢失 | **已修**：_reconnect 凭证回退 DB（重启生效） |
| 6 | P1 | _PARTIAL_DONE 单槽 vs 4 级分批冲突（浮盈锁 50% 先触发 → TP1/2/3 死） | 设计权衡，未改（文档已诚实化） |
| 7 | P2 | commission/swap 明细恒 0 | 未修 |

## 四、业界差距对照（2026 前沿，≥3 独立源交叉验证）

### 业界前沿图谱

| 系统/方法 | 机构/来源 | 核心方法 | 强项 |
|---|---|---|---|
| TradingAgents（80k★） | UCLA/MIT→Tauric | 多智能体模拟交易公司（分析师/牛熊辩论/风控/PM） | 结构辩论 + 风险团队（我们辩论环同源）|
| FinRL（15k★） | AI4Finance | 深度强化学习 + Alpaca 实盘 | 策略优化、在线学习 |
| FLAG-Trader / FinRL-DeepSeek（ACL 2025） | 学术 | LLM 信号 + RL 训练 | 混合显著优于单一方法 |
| P1GPT | 学术 | 多模态多智能体（技术/基本面/新闻+LLM 仲裁）| **risk-aware exit + adaptive re-entry**，回测 CR 16-32%、Sharpe 2.3-3.4 |
| QuantInsti DeepSeek 风控策略 | QuantInsti 2026 | **LLM 当风控经理决定仓位，不预测方向** + 月度 walk-forward + 硬护栏 | **与用户哲学完全一致**：方向预测不可靠，风险管理才是 LLM 用武之地 |
| StockBench | 清华 | LLM 交易代理基准 | 严肃检验"LLM 能否真赚钱" |

### 五维差距清单

| 维度 | 业界前沿做法 | 万象Ai 现状 | 差距 |
|---|---|---|---|
| **持仓管理** | P1GPT risk-aware exit；QuantInsti LLM 风控经理（仓位由 LLM 定+硬护栏）；TradingAgents Risk Team | **本轮刚落地**：position_action 篮子处置 + 回吐保护 + 防抖确认 | **0→1 完成**，仍需：RL 调权、月度再优化、组合级对冲 |
| **验证** | QuantInsti 月度 walk-forward + OOS（2023-2026 样本外）；shadow A/B | 辩论环 shadow 刚埋点（A/B 基线采集启动）；**无系统 walk-forward 回测框架** | **最大短板** |
| **决策** | LLM+RL 混合（FLAG-Trader）；agent 权重自适应（P1GPT 未来工作） | 静态加权门控 + meta_agent（DS/HY 权重固定） | 中 |
| **执行** | 事件级硬停（Fed/CPI/NFP）、slippage TCA、VWAP/TWAP 拆单 | 错峰抖动+幂等保正确性；**无执行质量度量** | 中 |
| **数据/校准** | 多模态（财报电话/预测市场/新闻级联）；**信号校准不可妥协**（90% 置信可能只有 55% 准）| 新闻部分注入、视觉 7b 新接入、calibrated_confidence 已有 | 中 |

### 业界共识（须正视）

- **回测 ≠ 实盘**：TradingAgents 回测年化 30.5%，30 天实盘仅 7% 且回撤 22%——实盘验证是唯一裁判（我们的前向验收铁律正确）
- **方向预测不可靠**（Chan 2024 / QuantInsti）："预测明天涨跌"没有模型有稳定 edge；**风险管理/仓位/退出才是 LLM 的可靠用途**——用户"开完仓核心就是维护持仓"的判断与业界 2026 结论不谋而合
- **LLM 静默退化**：市场 regime 变化时信号质量无声下降——须监控（我们有 degraded 监控）

## 五、最值得借鉴的 3 个升级方向

1. **补验证层（最优先）**：walk-forward 回测框架 + OOS 验证——辩论环 shadow 已在积累，扩展为全链路（篮子处置也进 shadow A/B：记录"若启用篮子 AI 会少亏多少"）
2. **持仓管理深化（用户方向，业界已证）**：从"LLM 出处置建议+规则执行"升级到 **QuantInsti 式"LLM 风控经理"**——LLM 直接定"当前仓位该多满"（50-100%），硬护栏（波动率/回撤止损）兜底；远期加 RL 调权
3. **执行质量工程化**：slippage TCA 归因 + 事件级硬停（FOMC/NFP/CPI 前后禁开新仓）

---
*审计方式：静态代码核查（Grep/Read）+ 运行时核查（health/DB/日志）+ 业界双源调研（WebSearch，独立源交叉验证）。本轮实施 1 项（篮子方案）+ 自查修复 2 bug；遗留 7 项待办已记录。*
